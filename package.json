{
  "name": "renthub",
  "version": "1.0.0",
  "description": "极简好用的开源房东出租记账与水电抄表系统，基于 Cloudflare Workers + D1 边缘无服务器架构",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute zufang_db --local --file=./migrations/0001_init.sql",
    "db:reset:local": "wrangler d1 execute zufang_db --local --command=\"DELETE FROM users; DELETE FROM leases; DELETE FROM payments; DELETE FROM attachments; DELETE FROM system_settings;\"",
    "db:migrate:remote": "wrangler d1 execute zufang_db --remote --file=./migrations/0001_init.sql",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250224.0",
    "typescript": "^5.7.3",
    "wrangler": "^3.111.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
