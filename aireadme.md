# 房东管家 (zufangwoker) - 快速上手与运行指南

## 一、 系统当前运行状态
- **本地服务地址**：`http://127.0.0.1:8787` (已在后台持续运行，浏览器打开即可直接使用)
- **数据库状态**：**已彻底清空重置为初始零数据状态**（`isInitialized: false`，保证无任何残留测试脏数据）
- **UI 规范**：100% 遵循 **Android 16 (Material 3 Expressive)** 设计语言，原生圆角大药丸、触控微震、微光渐变，全链路地道中文语境。
- **弹窗与排版优化**：门禁与各业务弹窗全面拓宽（最大宽度扩至 `max-w-2xl` / `max-w-3xl`），增大气泡间距与留白，完整适配手机触控（触摸热区 $\ge 44\text{px}$，防 iOS 页面自动放大）。
- **稳定性保障**：已彻底排查并修复内嵌脚本物理换行符引起的页面卡死问题，前端所有交互与标签切换丝滑流畅。

---

## 二、 核心功能与操作指南

### 1. 首次初始化向导 (零死锁、防被黑设计)
1. **设置主账号与密码**：
   - 输入管理员账号 (如: `admin`)；
   - 输入主密码 (至少 8 位，PBKDF2 强哈希派生加密密钥)；
2. **安全密保问题设置 (推荐设置，离线即用)**：
   - 下拉选择预设密保（如母校小学名称、出生城市全称、童年偶像）或输入自定义问题；
   - 填写密保答案（系统采用独立随机 Salt + PBKDF2 单向哈希，即便数据库泄露也无法被破解逆推）；
3. **安全邮箱与发信服务配置 (全量 SMTP 支持)**：
   - 展开“安全邮箱与邮件发信配置”；
   - **一键预设主流服务商**：提供 QQ邮箱、163网易邮箱、126邮箱、腾讯企业邮、网易企业邮、Gmail、Outlook 快捷芯片，一键自动填充服务器与端口（如 465 SSL/TLS 加密）；
   - **完整 SMTP 配置项**：
     - SMTP 服务器地址 (如 `smtp.qq.com`)
     - SMTP 端口 (465 或 587)
     - SSL/TLS 安全传输开关
     - 发件人账号 / 用户名
     - SMTP 密码 / 授权码 (QQ/163 请务必使用邮箱后台生成的专用授权码)
     - 发件人显示名称 (如 `房东管家`)
     - 发件人邮箱地址
     - 安全接收 / 找回邮箱 (接收测试验证码、找回密码邮件与催租提醒)
   - 点击 **“⚡ 发送实测验证码”**，边缘 SMTP 引擎直接建立 TCP 套接字向您的安全邮箱派发 6 位动态验证码；
   - 输入验证码点击“确认激活”，实时点亮 **`✓ 已实测激活`** 绿标！
   - *注：在此配置的 SMTP 发信通道会自动沉淀至系统设置，后续用于催租与水电账单自动派发。*
4. **绑定 2FA 动态口令 (支持跳过)**：
   - 点击“下一步：扫码绑定 2FA”；
   - 使用 Google Authenticator、微软身份验证器或 1Password 扫描生成的二维码；
   - 抄录保存自动生成的 **8 组紧急备用恢复码**（既可用于 2FA 应急，也可用于直接重置密码）；
   - 输入验证器显示的 6 位动态码激活；也可点击“暂不绑定，直接进入系统”。

---

### 2. 忘记密码安全找回 (多重立体防护)
如果您在登录时遗忘了管理员主密码：
1. 在登录界面点击 **“忘记密码？”**；
2. 输入管理员账号，点击 **“查询可用找回方式”**；
3. 系统根据您初始化时设置的凭证，展示可用通道：
   - **❓ 密保问题找回**：回答您设定的密保答案；
   - **🔑 紧急恢复码找回**：输入 8 组恢复码中的任意 1 组（验证通过后单次销毁防重放）；
   - **📧 安全邮箱找回**：点击“发送验证码”，由 SMTP 服务器实时向您绑定的安全邮箱派发 6 位动态码；
4. 验证通过后，系统下发 3 分钟有效期的防篡改签名 Token，直接输入并确认新主密码；
5. 重置成功后即可使用新密码登录！

---

### 3. 房源录入与下次收租日智能校准
- **剔除租客邮箱，聚焦房东管理**：房源录入彻底移除租客邮箱冗余项，所有提醒与通知均以房东待收视角为准；
- **防重复提交保护**：表单保存按钮内置防抖互斥锁，点击后立即锁定并展示加载中状态，防止多次连击生成重复房源；
- **起租与到期联动**：选择交租周期（年付/半年付/季付/月付）并录入起租日，到期日自动推算并扣除一天；
- **智能校准至到期下一年**：针对长租（如起租 2021、到期 2026、年付周期），系统智能自动将下次收租日校准到**到期日下一年**（`2027-09-24`），并提供快捷芯片：
  - `📅 到期下一年 (+1年)`
  - `🏁 合同到期日`
  - `⚡ 最近应收周期`
  - `🌱 起租下周期`

---

### 4. 房租收缴、水电抄表与付款凭据全链路绑定
- **收租与水电双模式凭据关联**：
  - **📤 上传新照片/PDF**：直接本地上传微信/支付宝转账截图或收据原件，AES-256 加密保存；
  - **🗃️ 选择内置凭据**：支持从之前已上传的历史凭证库中直接点选已有凭据，一键复用绑定至当前收租或水电账单，并支持图片即时缩略图预览；
- **记账日期与业务周期精确分级**：
  - **记账实收时间精确到秒 (`YYYY-MM-DD HH:mm:ss`)**：实收收款时间、水电抄表结算时间精确到时分秒，支持“⚡现在”一键快速填入当前精确时间，账单流水清晰还原交易瞬间；
  - **业务合同周期维持到日 (`YYYY-MM-DD`)**：下次收租日、合同起止日、水电计费周期等自然周期严格保持到日，避免时分秒干扰；
- **加密凭据解耦安全删除 (绝不误删账单)**：
  - 在加密凭据库或大图灯箱右上角点击“🗑️ 删除凭据”时，系统仅物理删除凭据原件本身；
  - **关联的房源资料与房租/水电财务流水账单 100% 完好保留，绝对不会误删任何财务账目**；
  - 删除后，流水卡片与凭据库即时联动刷新，自动清理关联，绝不残留任何失效死链；
- **水电抄表与欠缴管理**：录入本次底数自动计算用量与应收费用，状态可选 **`✓ 已收结清 (PAID)`** 或 **`🔴 待缴欠款 (UNPAID)`**；
- **一键结清**：若租客后续补缴水电费，在流水卡片中点击 **`✓ 结清`** 即可一键销账并同步解除房源卡片上的欠费警示。

---

### 5. 纯房东待收提醒 (Worker 使用者视角)
- **提醒视角纯正**：系统通知不再派发给租客，完全收敛至 Worker 使用者（房东安全邮箱），模板全面采用房东待收视角（如“您的房源【xxx】租金即将到期/存在水电欠费”）；
- **位于“系统设置”的“📧 待收提醒与邮箱设置”卡片**；
- 支持随时修改 SMTP 服务器与发件人配置，支持密码保留（掩码脱敏显示 `ab****op`，不修改密码无需重输）；
- 支持设置提前通知天数（预设快捷：提前7天、提前3天、提前1天，支持自定义天数组合）；
- 支持设置“交租当天发送”与“超期提醒发送”开关；
- 支持租金与水电通知自定义中文模板，提供丰富占位符（`{{承租人}}`、`{{房源名称}}`、`{{应交租金}}` 等），自带实时渲染预览盒；
- 支持 **“✉️ 发送测试邮件”** 与 **“⚡ 立即触发待收提醒检查”**。

---

### 6. 流畅度交互与弹窗便捷关闭
- **极速流畅与低开销**：消除重度高斯模糊滤镜，切换 Tab 时优先从本地内存即时渲染，杜绝滚动掉帧与卡顿；
- **点击空白处关闭**：所有弹窗均支持直接点击窗口外部的半透明空白遮罩区域平滑关闭；
- **ESC 全局快捷键**：在任意弹窗开启时按键盘 `ESC` 键即可一键关闭当前弹窗。

---

## 三、 本地常用运维命令
- **启动开发环境**：`npm run dev`
- **重置清空数据库**：`npm run db:reset:local`
- **执行本地数据库迁移**：`npm run db:migrate:local`
- **TypeScript 静态检查**：`npm run typecheck`
